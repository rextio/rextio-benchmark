# Torch CUDA profile

This is documentation-only scaffolding for a future Linux/WSL2 NVIDIA run.
CUDA is not built or measured by the `cpu` scripts, and this repository makes
no support, certification, or speedup claim from the macOS host.

Any future case must preserve `support_claim=false` and
`certification_ready=false` until separately promoted evidence says otherwise.

