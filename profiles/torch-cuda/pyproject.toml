[project]
name = "rextio-benchmark-torch-cuda-profile"
version = "0.0.0"
requires-python = "==3.11.*"
dependencies = [
  # Candidate 0.1.7 from the exact Git revision below — not a PyPI 0.1.7 release.
  "rextio==0.1.7",
  # Candidate 0.1.3 from the exact Git revision below — not a PyPI 0.1.3 release.
  "rextio-torch==0.1.3",
  "rextio-device-cuda==0.1.0",
  "torch==2.11.0",
  "numpy==2.4.6",
  "jsonschema==4.25.1",
]

[tool.uv]
package = false

[tool.uv.sources]
# Unreleased commit-pinned candidate build. Do not treat as PyPI rextio 0.1.7.
rextio = { git = "https://github.com/rextio/rextio", rev = "b8b8ed11f6b7b7aae4c7ae5205d88529608e8e97" }
# Unreleased commit-pinned candidate build. Do not treat as PyPI rextio-torch 0.1.3.
rextio-torch = { git = "https://github.com/rextio/rextio-torch", rev = "1e92b24b154c7266dc37d19533fc3e17a8b05f9a" }
