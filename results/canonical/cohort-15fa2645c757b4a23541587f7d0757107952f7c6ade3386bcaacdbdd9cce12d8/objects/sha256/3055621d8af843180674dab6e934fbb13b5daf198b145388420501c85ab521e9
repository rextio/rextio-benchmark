// Generated by Rextio. Do not edit manually.

#![allow(non_snake_case, unused_imports)]

use pyo3::prelude::*;
use pyo3::types::{PyDict, PyTuple};
use base64::Engine;
use sha2::Digest;
use std::collections::HashMap;
use std::collections::HashSet;

fn __rxtnp_dot1(a: &numpy::ndarray::Array1<f64>, b: &numpy::ndarray::Array1<f64>) -> pyo3::PyResult<f64> {
    if a.len() != b.len() {
        return Err(pyo3::exceptions::PyValueError::new_err(format!(
            "shapes ({},) and ({},) not aligned: {} (dim 0) != {} (dim 0)",
            a.len(), b.len(), a.len(), b.len()
        )));
    }
    Ok(a.dot(b))
}
fn __rxtnp_fmt_shape(shape: &[usize]) -> String {
    if shape.len() == 1 {
        return format!("({},)", shape[0]);
    }
    let parts: Vec<String> = shape.iter().map(|d| d.to_string()).collect();
    format!("({})", parts.join(","))
}
fn __rxtnp_broadcast_shape(a: &[usize], b: &[usize]) -> pyo3::PyResult<Vec<usize>> {
    let ndim = a.len().max(b.len());
    let mut out = Vec::with_capacity(ndim);
    for i in 0..ndim {
        let da = if i + a.len() < ndim { 1 } else { a[i + a.len() - ndim] };
        let db = if i + b.len() < ndim { 1 } else { b[i + b.len() - ndim] };
        if da == db {
            out.push(da);
        } else if da == 1 {
            out.push(db);
        } else if db == 1 {
            out.push(da);
        } else {
            return Err(pyo3::exceptions::PyValueError::new_err(format!(
                "operands could not be broadcast together with shapes {} {} ",
                __rxtnp_fmt_shape(a), __rxtnp_fmt_shape(b)
            )));
        }
    }
    Ok(out)
}
fn __rxtnp_echain_f64_r1_Bmul_Badd_L0r1_L1r1_Bsub_L2r1_L3r1(a0: &numpy::ndarray::Array1<f64>, a1: &numpy::ndarray::Array1<f64>, a2: &numpy::ndarray::Array1<f64>, a3: &numpy::ndarray::Array1<f64>) -> pyo3::PyResult<numpy::ndarray::Array1<f64>> {
    let s0 = __rxtnp_broadcast_shape(a0.shape(), a1.shape())?;
    let s1 = __rxtnp_broadcast_shape(a2.shape(), a3.shape())?;
    let s2 = __rxtnp_broadcast_shape(s0.as_slice(), s1.as_slice())?;
    if s2.len() != 1 {
        return Err(pyo3::exceptions::PyValueError::new_err(
            "rextio-numpy: fused rank-1 result has non-1-D shape"
        ));
    }
    let dim = numpy::ndarray::Ix1(s2[0]);
    let v0 = a0.broadcast(dim).ok_or_else(|| {
        pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes {} {} ",
            __rxtnp_fmt_shape(a0.shape()), __rxtnp_fmt_shape(s2.as_slice())
        ))
    })?;
    let v1 = a1.broadcast(dim).ok_or_else(|| {
        pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes {} {} ",
            __rxtnp_fmt_shape(a1.shape()), __rxtnp_fmt_shape(s2.as_slice())
        ))
    })?;
    let v2 = a2.broadcast(dim).ok_or_else(|| {
        pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes {} {} ",
            __rxtnp_fmt_shape(a2.shape()), __rxtnp_fmt_shape(s2.as_slice())
        ))
    })?;
    let v3 = a3.broadcast(dim).ok_or_else(|| {
        pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes {} {} ",
            __rxtnp_fmt_shape(a3.shape()), __rxtnp_fmt_shape(s2.as_slice())
        ))
    })?;
    let out = numpy::ndarray::Array1::from_shape_fn(dim, |i| {
        let x0 = v0[i];
        let x1 = v1[i];
        let x2 = v2[i];
        let x3 = v3[i];
        let t0 = x0 + x1;
        let t1 = x2 - x3;
        t0 * t1
    });
    Ok(out)
}
fn __rxtnp_sub1_aa(a: &numpy::ndarray::Array1<f64>, b: &numpy::ndarray::Array1<f64>) -> pyo3::PyResult<numpy::ndarray::Array1<f64>> {
    if a.len() == b.len() {
        return Ok(a - b);
    }
    if a.len() == 1 {
        // broadcast: s comes from `a`, so it is the LEFT operand
        let s = a[0];
        return Ok(b.mapv(|x| s - x));
    }
    if b.len() == 1 {
        // broadcast: s comes from `b`, so it is the RIGHT operand
        // (operand order matters for the non-commutative - and /)
        let s = b[0];
        return Ok(a.mapv(|x| x - s));
    }
    Err(pyo3::exceptions::PyValueError::new_err(format!(
        "operands could not be broadcast together with shapes ({},) ({},) ",
        a.len(), b.len()
    )))
}
fn __rxtnp_add1_as(a: &numpy::ndarray::Array1<f64>, s: f64) -> pyo3::PyResult<numpy::ndarray::Array1<f64>> {
    Ok(a.mapv(|x| x + s))
}
fn __rxtnp_div1_aa(a: &numpy::ndarray::Array1<f64>, b: &numpy::ndarray::Array1<f64>) -> pyo3::PyResult<numpy::ndarray::Array1<f64>> {
    if a.len() == b.len() {
        return Ok(a / b);
    }
    if a.len() == 1 {
        // broadcast: s comes from `a`, so it is the LEFT operand
        let s = a[0];
        return Ok(b.mapv(|x| s / x));
    }
    if b.len() == 1 {
        // broadcast: s comes from `b`, so it is the RIGHT operand
        // (operand order matters for the non-commutative - and /)
        let s = b[0];
        return Ok(a.mapv(|x| x / s));
    }
    Err(pyo3::exceptions::PyValueError::new_err(format!(
        "operands could not be broadcast together with shapes ({},) ({},) ",
        a.len(), b.len()
    )))
}
fn __rextio_checked_rem(a: i64, b: i64) -> PyResult<i64> {
    if b == 0 { return Err(pyo3::exceptions::PyZeroDivisionError::new_err("integer modulo by zero")); }
    let r = a.checked_rem(b).unwrap_or(0);
    Ok(if r != 0 && (r ^ b) < 0 { r + b } else { r })
}


fn rextio_call_python_runtime(
    py: Python<'_>,
    module_name: &str,
    ordinal: usize,
    args: &Bound<'_, PyTuple>,
    kwargs: Option<&Bound<'_, PyDict>>,
) -> PyResult<Py<PyAny>> {
    let registry = PyModule::import(py, "rextio.runtime.original_registry")?;
    let target = registry.getattr("resolve_runtime_original")?.call1((module_name, ordinal))?;
    let value = target.call(args, kwargs)?;
    Ok(value.unbind())
}

#[allow(unused_variables)]
#[pyfunction]
fn numpy_case__workload__blas_dot<'py>(py: pyo3::Python<'py>, left: numpy::PyReadonlyArray1<'py, f64>, right: numpy::PyReadonlyArray1<'py, f64>) -> PyResult<f64> {
    let left = { if !left.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } left.as_array().to_owned() };
    let right = { if !right.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } right.as_array().to_owned() };
    return Ok(__rxtnp_dot1(&left, &right)?);
}

#[allow(unused_variables)]
#[pyfunction]
fn numpy_case__workload__mixed_fusion<'py>(py: pyo3::Python<'py>, left: numpy::PyReadonlyArray1<'py, f64>, right: numpy::PyReadonlyArray1<'py, f64>, phase: i64) -> PyResult<pyo3::Bound<'py, numpy::PyArray1<f64>>> {
    let left = { if !left.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } left.as_array().to_owned() };
    let right = { if !right.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } right.as_array().to_owned() };
    if __rextio_checked_rem(phase, 2)? == 0 {
        return Ok(numpy::ToPyArray::to_pyarray(&__rxtnp_echain_f64_r1_Bmul_Badd_L0r1_L1r1_Bsub_L2r1_L3r1(&left, &right, &left, &right)?, py));
    }
    return Ok(numpy::ToPyArray::to_pyarray(&__rxtnp_div1_aa(&(__rxtnp_sub1_aa(&left, &right)?), &(__rxtnp_add1_as(&right, 2.0)?))?, py));
}

#[pymodule]
fn _rextio_native(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_function(wrap_pyfunction!(numpy_case__workload__blas_dot, m)?)?;
    m.add_function(wrap_pyfunction!(numpy_case__workload__mixed_fusion, m)?)?;
    Ok(())
}
