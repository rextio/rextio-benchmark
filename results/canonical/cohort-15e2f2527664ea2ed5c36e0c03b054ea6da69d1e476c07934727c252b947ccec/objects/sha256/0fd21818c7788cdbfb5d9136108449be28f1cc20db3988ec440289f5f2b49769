// Generated by Rextio. Do not edit manually.

#![allow(non_snake_case, unused_imports)]

use pyo3::prelude::*;
use pyo3::types::{PyDict, PyTuple};
use base64::Engine;
use sha2::Digest;
use std::collections::HashMap;
use std::collections::HashSet;

fn __rxtnp_f64_1d_output<'py, F>(
    py: pyo3::Python<'py>,
    len: usize,
    fill: F,
) -> pyo3::PyResult<numpy::PyReadonlyArray1<'py, f64>>
where
    F: FnOnce(&mut [f64]) -> pyo3::PyResult<()>,
{
    let out = numpy::PyArray1::<f64>::zeros(py, len, false);
    {
        let mut writable = numpy::PyArrayMethods::try_readwrite(&out).map_err(|error| {
            pyo3::exceptions::PyRuntimeError::new_err(format!(
                "rextio-numpy: fresh f64 rank-1 output borrow failed: {error}"
            ))
        })?;
        let values = writable.as_slice_mut().map_err(|error| {
            pyo3::exceptions::PyRuntimeError::new_err(format!(
                "rextio-numpy: fresh f64 rank-1 output is not contiguous: {error}"
            ))
        })?;
        fill(values)?;
    }
    numpy::PyArrayMethods::try_into_readonly(out).map_err(|error| {
        pyo3::exceptions::PyRuntimeError::new_err(format!(
            "rextio-numpy: fresh f64 rank-1 output freeze failed: {error}"
        ))
    })
}
fn __rxtnp_release_f64_1d<'py>(
    value: numpy::PyReadonlyArray1<'py, f64>,
) -> pyo3::PyResult<pyo3::Bound<'py, numpy::PyArray1<f64>>> {
    let owner = (*value).clone();
    drop(value);
    Ok(owner)
}
fn __rxtnp_dot1<'py>(a: &numpy::PyReadonlyArray1<'py, f64>, b: &numpy::PyReadonlyArray1<'py, f64>) -> pyo3::PyResult<f64> {
    let a = a.as_array();
    let b = b.as_array();
    if a.len() != b.len() {
        return Err(pyo3::exceptions::PyValueError::new_err(format!(
            "shapes ({},) and ({},) not aligned: {} (dim 0) != {} (dim 0)",
            a.len(), b.len(), a.len(), b.len()
        )));
    }
    Ok(a.dot(&b))
}
fn __rxtnp_add1_as<'py>(py: pyo3::Python<'py>, a: &numpy::PyReadonlyArray1<'py, f64>, s: f64) -> pyo3::PyResult<numpy::PyReadonlyArray1<'py, f64>> {
    let a = a.as_array();
    __rxtnp_f64_1d_output(py, a.len(), |out| {
        for (i, value) in out.iter_mut().enumerate() {
            let x = a[i];
            *value = x + s;
        }
        Ok(())
    })
}
fn __rxtnp_fmt_shape(shape: &[usize]) -> String {
    if shape.len() == 1 {
        return format!("({},)", shape[0]);
    }
    let parts: Vec<String> = shape.iter().map(|d| d.to_string()).collect();
    format!("({})", parts.join(","))
}
fn __rxtnp_broadcast_shape(a: &[usize], b: &[usize]) -> pyo3::PyResult<Vec<usize>> {
    let ndim = a.len().max(b.len());
    let mut out = Vec::with_capacity(ndim);
    for i in 0..ndim {
        let da = if i + a.len() < ndim { 1 } else { a[i + a.len() - ndim] };
        let db = if i + b.len() < ndim { 1 } else { b[i + b.len() - ndim] };
        if da == db {
            out.push(da);
        } else if da == 1 {
            out.push(db);
        } else if db == 1 {
            out.push(da);
        } else {
            return Err(pyo3::exceptions::PyValueError::new_err(format!(
                "operands could not be broadcast together with shapes {} {} ",
                __rxtnp_fmt_shape(a), __rxtnp_fmt_shape(b)
            )));
        }
    }
    Ok(out)
}
fn __rxtnp_echain_f64_r1_Bmul_Badd_L0r1_L1r1_Bsub_L2r1_L3r1_al_0_1_0_1<'py>(py: pyo3::Python<'py>, a0: &numpy::PyReadonlyArray1<'py, f64>, a1: &numpy::PyReadonlyArray1<'py, f64>) -> pyo3::PyResult<numpy::PyReadonlyArray1<'py, f64>> {
    let a0 = a0.as_array();
    let a1 = a1.as_array();
    // Equal-shape standard-layout fast path (before broadcast-shape Vec work).
    if a0.shape() == a1.shape() && a0.is_standard_layout() && a1.is_standard_layout() {
        if let (Some(sl0), Some(sl1)) = (a0.as_slice(), a1.as_slice()) {
            return __rxtnp_f64_1d_output(py, a0.shape()[0], |out| {
                for i in 0..out.len() {
                let x0 = sl0[i];
                let x1 = sl1[i];
                let x2 = x0;
                let x3 = x1;
                    let t0 = x0 + x1;
                    let t1 = x2 - x3;
                    out[i] = t0 * t1;
                }
                Ok(())
            });
        }
    }
    let s0 = __rxtnp_broadcast_shape(a0.shape(), a1.shape())?;
    let s1 = __rxtnp_broadcast_shape(a0.shape(), a1.shape())?;
    let s2 = __rxtnp_broadcast_shape(s0.as_slice(), s1.as_slice())?;
    if s2.len() != 1 {
        return Err(pyo3::exceptions::PyValueError::new_err(
            "rextio-numpy: fused rank-1 result has non-1-D shape"
        ));
    }
    let dim = numpy::ndarray::Ix1(s2[0]);
    let v0 = a0.broadcast(dim).ok_or_else(|| {
        pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes {} {} ",
            __rxtnp_fmt_shape(a0.shape()), __rxtnp_fmt_shape(s2.as_slice())
        ))
    })?;
    let v1 = a1.broadcast(dim).ok_or_else(|| {
        pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes {} {} ",
            __rxtnp_fmt_shape(a1.shape()), __rxtnp_fmt_shape(s2.as_slice())
        ))
    })?;
    __rxtnp_f64_1d_output(py, s2[0], |out| {
        for i in 0..out.len() {
            let x0 = v0[i];
            let x1 = v1[i];
            let x2 = x0;
            let x3 = x1;
            let t0 = x0 + x1;
            let t1 = x2 - x3;
            out[i] = t0 * t1;
        }
        Ok(())
    })
}
fn __rxtnp_sub1_aa<'py>(py: pyo3::Python<'py>, a: &numpy::PyReadonlyArray1<'py, f64>, b: &numpy::PyReadonlyArray1<'py, f64>) -> pyo3::PyResult<numpy::PyReadonlyArray1<'py, f64>> {
    let a = a.as_array();
    let b = b.as_array();
    let n = if a.len() == b.len() {
        a.len()
    } else if a.len() == 1 {
        b.len()
    } else if b.len() == 1 {
        a.len()
    } else {
        return Err(pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes ({},) ({},) ",
            a.len(), b.len()
        )));
    };
    __rxtnp_f64_1d_output(py, n, |out| {
        for i in 0..n {
            let x = if a.len() == 1 { a[0] } else { a[i] };
            let y = if b.len() == 1 { b[0] } else { b[i] };
            out[i] = x - y;
        }
        Ok(())
    })
}
fn __rxtnp_div1_aa<'py>(py: pyo3::Python<'py>, a: &numpy::PyReadonlyArray1<'py, f64>, b: &numpy::PyReadonlyArray1<'py, f64>) -> pyo3::PyResult<numpy::PyReadonlyArray1<'py, f64>> {
    let a = a.as_array();
    let b = b.as_array();
    let n = if a.len() == b.len() {
        a.len()
    } else if a.len() == 1 {
        b.len()
    } else if b.len() == 1 {
        a.len()
    } else {
        return Err(pyo3::exceptions::PyValueError::new_err(format!(
            "operands could not be broadcast together with shapes ({},) ({},) ",
            a.len(), b.len()
        )));
    };
    __rxtnp_f64_1d_output(py, n, |out| {
        for i in 0..n {
            let x = if a.len() == 1 { a[0] } else { a[i] };
            let y = if b.len() == 1 { b[0] } else { b[i] };
            out[i] = x / y;
        }
        Ok(())
    })
}
fn __rextio_checked_rem(a: i64, b: i64) -> PyResult<i64> {
    if b == 0 { return Err(pyo3::exceptions::PyZeroDivisionError::new_err("integer modulo by zero")); }
    let r = a.checked_rem(b).unwrap_or(0);
    Ok(if r != 0 && (r ^ b) < 0 { r + b } else { r })
}


fn rextio_call_python_runtime(
    py: Python<'_>,
    module_name: &str,
    ordinal: usize,
    args: &Bound<'_, PyTuple>,
    kwargs: Option<&Bound<'_, PyDict>>,
) -> PyResult<Py<PyAny>> {
    let registry = PyModule::import(py, "rextio.runtime.original_registry")?;
    let target = registry.getattr("resolve_runtime_original")?.call1((module_name, ordinal))?;
    let value = target.call(args, kwargs)?;
    Ok(value.unbind())
}

#[allow(unused_variables)]
#[pyfunction]
fn numpy_case__workload__blas_dot<'py>(py: pyo3::Python<'py>, left: numpy::PyReadonlyArray1<'py, f64>, right: numpy::PyReadonlyArray1<'py, f64>) -> PyResult<f64> {
    let left = { if !left.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } left };
    let right = { if !right.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } right };
    return Ok(__rxtnp_dot1(&left, &right)?);
}

#[allow(unused_variables)]
#[pyfunction]
fn numpy_case__workload__boundary_direct_sink<'py>(py: pyo3::Python<'py>, values: numpy::PyReadonlyArray1<'py, f64>) -> PyResult<pyo3::Bound<'py, numpy::PyArray1<f64>>> {
    let values = { if !values.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } values };
    return Ok(__rxtnp_release_f64_1d(__rxtnp_add1_as(py, &values, 0.25)?)?);
}

#[allow(unused_variables)]
#[pyfunction]
fn numpy_case__workload__mixed_fusion<'py>(py: pyo3::Python<'py>, left: numpy::PyReadonlyArray1<'py, f64>, right: numpy::PyReadonlyArray1<'py, f64>, phase: i64) -> PyResult<pyo3::Bound<'py, numpy::PyArray1<f64>>> {
    let left = { if !left.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } left };
    let right = { if !right.is_exact_instance_of::<numpy::PyArray1<f64>>() { return Err(pyo3::exceptions::PyTypeError::new_err("rextio-numpy native boundary requires exact numpy.ndarray; ndarray subclasses are unsupported")); } right };
    if __rextio_checked_rem(phase, 2)? == 0 {
        return Ok(__rxtnp_release_f64_1d(__rxtnp_echain_f64_r1_Bmul_Badd_L0r1_L1r1_Bsub_L2r1_L3r1_al_0_1_0_1(py, &left, &right)?)?);
    }
    return Ok(__rxtnp_release_f64_1d(__rxtnp_div1_aa(py, &(__rxtnp_sub1_aa(py, &left, &right)?), &(__rxtnp_add1_as(py, &right, 2.0)?))?)?);
}

#[pymodule]
fn _rextio_native(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_function(wrap_pyfunction!(numpy_case__workload__blas_dot, m)?)?;
    m.add_function(wrap_pyfunction!(numpy_case__workload__boundary_direct_sink, m)?)?;
    m.add_function(wrap_pyfunction!(numpy_case__workload__mixed_fusion, m)?)?;
    Ok(())
}
